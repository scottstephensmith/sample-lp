{
  "name": "sample-cursor-site",
  "version": "1.0.0",
  "description": "Sample Cursor Site - A simple static site generator",
  "scripts": {
    "build": "node scripts/build.js",
    "serve": "npm run build && live-server dist",
    "start": "npm run serve"
  },
  "dependencies": {
    "fs-extra": "^11.1.1",
    "gray-matter": "^4.0.3",
    "marked": "^4.3.0"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
